function addNumber(){
    let number = parseInt(prompt("Enter the number"));
    for(let i = 1;i <=10; i++) {
        let res = number * 1;
        console.log(number);
        document.write(res,'\n');

        //document.getElementById("table").innerHTML=res +'<br>'

    }
}